// script.js

document.getElementById("addQuoteBtn").addEventListener("click", addQuote);

function addQuote() {
  // Handle the form submission here using JavaScript or AJAX if needed
  console.log("Quote submission triggered!");
}
